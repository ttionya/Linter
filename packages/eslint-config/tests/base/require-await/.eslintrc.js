module.exports = {
  rules: {
    /**
     * async 函数中必须存在 await 语句
     * @see {@link https://eslint.org/docs/latest/rules/require-await}
     * @category Base
     */
    'require-await': 'off',
  },
}
