module.exports = {
  rules: {
    /**
     * async 函数中必须存在 await 语句
     * @see {@link https://eslint.org/docs/v8.x/rules/require-await}
     * @category Base
     */
    'require-await': 'off',
  },
}
