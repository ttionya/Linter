module.exports = {
  rules: {
    /**
     * 必须使用模版字符串而不是字符串连接
     * @see {@link https://eslint.org/docs/latest/rules/prefer-template}
     * @category Base
     * @fixable
     */
    'prefer-template': 'off',
  },
}
