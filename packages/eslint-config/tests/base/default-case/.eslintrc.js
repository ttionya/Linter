module.exports = {
  rules: {
    /**
     * switch 必须使用 default，即使为空也要有注释
     * @see {@link https://eslint.org/docs/latest/rules/default-case}
     * @category Base
     */
    'default-case': 'error',
  },
}
