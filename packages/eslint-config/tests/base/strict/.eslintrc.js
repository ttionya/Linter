module.exports = {
  rules: {
    /**
     * 根据情况使用严格模式
     * @see {@link https://eslint.org/docs/latest/rules/strict}
     * @category Base
     * @fixable
     */
    strict: ['error', 'safe'],
  },
}
