{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "npmClient": "npm",
  "command": {
    "publish": {
      "ignoreChanges": ["*.md"]
    },
    "version": {
      "allowBranch": "master",
      "message": "chore(release): publish %s"
    }
  }
}
